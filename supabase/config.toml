# Pass Authorization header through to the function; we validate the JWT inside the function.
# See: https://supabase.com/docs/guides/functions/function-configuration#skipping-authorization-checks
[functions.deploy-openclaw]
verify_jwt = false
